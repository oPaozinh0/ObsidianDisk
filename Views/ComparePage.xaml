<UserControl x:Class="ObsidianDisk.Views.ComparePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:ObsidianDisk.Controls">
    <Border Style="{StaticResource Card}" Padding="16,12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" LastChildFill="False">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <TextBlock x:Name="BusyText" Style="{StaticResource MutedText}" Margin="0,0,12,0"
                               VerticalAlignment="Center"/>
                    <controls:ObsidianButton Variant="Primary" Height="32" Click="Compare_Click"
                            Content="{StaticResource Cmp.Compare}"/>
                </StackPanel>
                <TextBlock DockPanel.Dock="Left" Style="{StaticResource CardTitle}"
                           Text="{StaticResource Cmp.Title}" VerticalAlignment="Center"/>
            </DockPanel>

            <TextBlock x:Name="DescText" Grid.Row="1" Style="{StaticResource MutedText}" TextWrapping="Wrap"
                       Margin="0,8,0,10" Text="{StaticResource Cmp.Desc}"/>

            <!-- Seletores das duas pastas -->
            <Grid Grid.Row="2" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <controls:ObsidianButton x:Name="PickAButton" Grid.Column="0" Variant="Ghost" Height="34"
                        HorizontalContentAlignment="Left" Padding="12,0" Click="PickA_Click"
                        Content="{StaticResource Cmp.PickA}"/>
                <controls:ObsidianButton x:Name="PickBButton" Grid.Column="2" Variant="Ghost" Height="34"
                        HorizontalContentAlignment="Left" Padding="12,0" Click="PickB_Click"
                        Content="{StaticResource Cmp.PickB}"/>
            </Grid>

            <!-- Resultado lado a lado -->
            <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="14"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel x:Name="ColumnA" Grid.Column="0"/>
                    <StackPanel x:Name="ColumnB" Grid.Column="2"/>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
